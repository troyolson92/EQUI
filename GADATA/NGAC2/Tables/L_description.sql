CREATE TABLE [NGAC2].[L_description] (
    [id]          INT           IDENTITY (1, 1) NOT NULL,
    [Description] VARCHAR (MAX) NULL,
    CONSTRAINT [PK_L_description] PRIMARY KEY CLUSTERED ([id] ASC)
);

