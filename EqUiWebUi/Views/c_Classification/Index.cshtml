@model IEnumerable<EqUiWebUi.Models.c_Classification>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
@Html.Grid(Model).Named("c_ClassificationGrid").Build(columns =>
{
    columns.Add(c => c.id).Titled("id").InitialSort(GridSortOrder.Asc);
    columns.Add(c => c.Classification).Titled("Classification");
    columns.Add(c => c.Discription).Titled("Discription");
    columns.Add(c => c)
                    .Titled("")
                    .Encoded(false)
                .RenderedAs(c => MyFilters.IconActionLink(Url.Action("Edit", new { id = c.id }), "fas fa-pencil-alt", "", "Edit this rule") + " | " +
                                 MyFilters.IconActionLink(Url.Action("Delete", new { id = c.id }), "fas fa-trash-alt", "", "Delete this rule")
                    ).Filterable(false);
}).Filterable().Sortable().Pageable(pager =>
{
    pager.RowsPerPage = 10;
}).Empty("Did not find anything").Css("table-hover")
