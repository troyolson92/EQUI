@model IQueryable<EqUiWebUi.Areas.Welding.Models.ComparePitchV316>
@{
    ViewBag.Title = "ComparePitchV316";
}

@Html.Grid(Model).Named("ComparePitchV316").Build(columns =>
{
    columns.Add(c => c.timerNumber).Titled("timernumber").Formatted("{0:dd-MM-yyyy HH:mm:ss}");
    columns.Add(c => c.Timer).Titled("timer").InitialSort(GridSortOrder.Desc);
    columns.Add(c => c.SpotID).Titled("spotidFromPitch");
 
    columns.Add(c => c.AlternativeNumber).Titled("SpotInTimer");
    columns.Add(c => c.lastWeldTime).Titled("Last30min_productionStatus").Formatted("{0:dd-MM-yyyy HH:mm:ss}");
    columns.Add(c => c.lastULControleTime).Titled("lastULControleTime");
    columns.Add(c => c.Expr1).Titled("variant");


}).Filterable().Pageable(pager =>
{
    pager.RowsPerPage = 20;

}
        ).Sortable().Css("table-hover").RowCss(model => model.AlternativeNumber != null ? "ani_PulseRed" : "");

